import test from "@playwright/test";

test.describe("Order summary", function () {
    test("Should be able to download the order summary and validate order details from the downloaded pdf summary", async ({
        page
    }) => { });
});
